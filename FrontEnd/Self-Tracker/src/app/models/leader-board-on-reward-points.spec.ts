import { LeaderBoardOnRewardPoints } from './leader-board-on-reward-points';

describe('LeaderBoardOnRewardPoints', () => {
  it('should create an instance', () => {
    expect(new LeaderBoardOnRewardPoints()).toBeTruthy();
  });
});
