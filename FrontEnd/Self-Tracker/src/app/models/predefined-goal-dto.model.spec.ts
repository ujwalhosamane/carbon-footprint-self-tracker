import { PredefinedGoalDto } from './predefined-goal-dto.model';

describe('PredefinedGoalDto', () => {
  it('should create an instance', () => {
    expect(new PredefinedGoalDto()).toBeTruthy();
  });
});
